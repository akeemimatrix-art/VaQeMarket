import os

PROBE_INTERVAL_SECONDS = int(os.getenv("PROBE_INTERVAL_SECONDS", "30"))
CONSENSUS_INTERVAL_SECONDS = int(os.getenv("CONSENSUS_INTERVAL_SECONDS", "30"))

SENTINEL_WEIGHT = 1.0
VALIDATOR_WEIGHT = 2.5
ANCHOR_WEIGHT = 5.0

OUTLIER_STDDEV_LIMIT = 2.5

CANDLE_TIMEFRAMES = {
    "1m": 60,
    "5m": 300,
    "15m": 900,
    "1h": 3600,
    "4h": 14400,
    "1d": 86400,
}
